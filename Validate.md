# Local validation checklist

- [ ] `cd backend-laravel` → `php artisan test` (if you use tests)
- [ ] `cd backend-laravel` → API up: `php artisan serve` — hit `GET /api/students`
- [ ] `cd frontend-react` → `npm run lint` and `npm run build`
- [ ] Create / edit / delete a student in the UI against the running API
